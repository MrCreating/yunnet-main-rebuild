# config/local/docker/context/postfix

Контекст для сборки Postfix (почта)

### Схема проекта
- `entrypoint.sh` - скрипт для запуска после сборки
- `set-exim4-update-conf` - один из конфигов для повышения надежности писем...